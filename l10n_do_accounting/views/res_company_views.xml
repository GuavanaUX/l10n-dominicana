<?xml version="1.0" encoding="utf-8"?>
<odoo>

    <record id="inherit_view_company_form" model="ir.ui.view">
        <field name="name">inherit.view.company.form</field>
        <field name="model">res.company</field>
        <field name="inherit_id" ref="base.view_company_form"/>
        <field name="arch" type="xml">

            <!-- Odoo 18 uses RNC as default string for the field display name -->

            <!-- 
            <xpath expr="//field[@name='vat']" position="attributes">
                <attribute name="invisible">country_id == ref('base.do')</attribute>
            </xpath> 
            -->

            <!-- 
            <xpath expr="//field[@name='vat']" position="after">
                <field name="vat" placeholder="ej. 123456789" string="RNC"
                    invisible="country_id != ref('base.do')" />
            </xpath> 
            -->

        </field>
    </record>

</odoo>
